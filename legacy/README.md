# Legacy 

This directory contains some older code that needs to be refactored. In particular, it contains the code necessary for a VERY large-scale implementation of Algorithm 3.2. In a very large run, data cannot be stored in memory but must be continuously dumped to files. That is essentially the only difference between it and other code contained in this repo. 